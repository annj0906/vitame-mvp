# VITAME 모바일 웹 프로젝트 PRD

## 최신 시안 적용 — 2026-09-21 (이하 기존 설계보다 우선)

- 2026-09-26 분석 시안: 종합 분석 / AI 가이드 탭을 제공한다. 종합 분석은 월 선택, 월별 예시 점수 막대 그래프와 클릭 시 전월 대비 말풍선, 부족 4 / 주의 2 / 적정 6 영양소 카드를 표시한다. 각 카드는 독립적으로 접고 펼치며 접으면 영양소 이름 태그를 표시한다. 점수와 영양소 수치는 의료 평가가 아닌 예시 데이터다.
- 2026-09-26 AI 가이드 시안: 보라 캐릭터 주위의 건강 영역 버튼과 관심 건강 태그를 누르면 해당 가이드 섹션으로 빠르게 이동한다. 기존 MY 관심 건강을 우선 표시하고, 각 섹션은 현재 상태, 복용·식습관·생활 습관 안내, 관련 예시 제품을 제공한다. 관심 건강을 선택하지 않아도 모든 가이드를 볼 수 있다. 로컬 수정만 진행하며 배포는 별도 요청에 따른다.

- 최신 온보딩 시안: 로고를 축소해 건너뛰기와 같은 상단 행에 세로 중앙 정렬한다. 캐릭터의 거친 질감과 바닥 그림자를 제거한다. 캐릭터·제목·설명을 하나의 패널로 묶어 버튼/스와이프 시 수평 슬라이드한다. 활성 페이지 표시는 짧은 검정 막대, 나머지는 회색 점이다. 복용 빈도는 5개 흰 스냅 지점과 단계별 캐릭터(분홍 별/무지개/주황 네잎/보라 꽃/파란 오각형)를 사용하며 이동·캐릭터 전환을 부드럽게 한다.

- 2026-09-23 4단계 온보딩: 관심 건강(1/4) → 복용 빈도(2/4, 5단계 슬라이더·젤리 손잡이) → 어려운 점(3/4, 단일 선택 4항목) → 등록 안내(4/4). 1~3단계는 선택 없이 건너뛸 수 있다. 다음으로는 응답을 기기에 저장하고 뒤로 이동 시 유지한다. 등록 안내는 스캔/직접 입력/나중에(홈)로 분기한다. 실제 OCR 미제공을 명시하고 기존 로그인→홈 흐름을 유지한다.

- 2026-09-23 최우선 흐름: 온보딩 마지막 시작하기 및 상단 건너뛰기는 선택형 관심 건강 화면으로 이동한다. 관심 건강은 다음으로 저장 또는 건너뛰기 후 영양제 등록으로 이어진다. 기존 MY 관심 건강 관리도 유지한다. 밑줄 로그인만 로그인으로 이동하며 로그인 후 홈으로 간다. 로그인/회원가입 상호 링크를 제공한다. 회원가입은 아이디 형식 확인 및 비밀번호 일치 검증을 제공하는 목업으로 실제 중복조회·계정 생성은 하지 않으며 자격정보는 저장/전송하지 않는다. 회원가입 체험 완료 후 로그인 화면으로 이동한다.

- 온보딩 최신 확정: 인트로 로고 완료 이벤트 후 같은 로고를 상단으로 이동·축소해 연결한다. 세 장은 보라 꽃/무지개 젤리/분홍 별 캐릭터와 부유·젤리 모션을 사용한다. 페이지 점은 원형이고 1·2장 CTA는 다음으로, 마지막은 시작하기다. 로그인은 밝은 테마로 이메일·비밀번호·보조 옵션·소셜 버튼·회원가입 배치를 적용하고 실제 인증 없는 체험임을 표시한다. 비밀번호를 저장/전송하지 않으며 로그인 후 홈으로 바로 이동한다.

- 최신 모션 규칙: 복용 현황은 날짜/완료 수/전체 수가 달라질 때만 재생하며 정렬·편집에는 정지 상태를 유지한다. 삭제 안내는 4.5초 후 사라지고 복용 목록의 복구 버튼으로 계속 복원할 수 있다. 알림 진입은 오른쪽에서 왼쪽, 복귀는 오른쪽으로 퇴장한다. 홈 건강점수 제목은 선택 날짜를 반영하고, 예시 점수는 날짜와 완료 비율로 결정되는 안정적인 목업 값이다(의료 평가 아님). 다색 링/숫자는 날짜·복용 기록 변경 때만 재생한다.

- 최신 피드백: 홈 영양 안내의 벨을 축소하고 독립 닫기를 제공한다(현재 방문 동안 유지). 복용 현황은 연한 민트 단일 카드와 실제 완료 수/달성률 및 숫자·가로 막대 상승 애니메이션으로 표시한다. 복용 목록에 시간순 정렬과 작은 스와이프 안내를 추가한다. 좌우 스와이프 노출 폭은 72px로 동일하게 제한하고 편집/삭제 SVG는 24px로 통일한다. 모션 감소 환경에서는 애니메이션을 끈다.

- 2026-09-22 추가 피드백(최우선): 홈 오늘 버튼은 월간 펼침 상태에서만 보인다. 홈 복용은 카드/목록 보기 전환을 지원하며 반대 보기 아이콘 하나만 표시한다. 복용 주간/월간 세그먼트는 오늘 버튼과 무관하게 화면 가운데 정렬한다. 복용 항목은 분리된 라운드 카드로 표시하며 오른쪽 스와이프는 즉시 상세 편집으로 이동한다. 편집 모드 드래그 중 주변 카드가 자리를 비우고 놓으면 순서가 저장된다. 알약 색상은 항목에 고정되어 순서 이동 시 함께 이동한다.

- 복용 편집 시 행 전체 또는 손잡이 드래그로 순서를 저장한다. 일반 모드에서 왼쪽으로 60px 이상 밀면 즉시 삭제하며 저장된 되돌리기로 복구한다. 홈/복용 달력에 오늘 복귀 버튼을 제공하고 스와이프는 손가락을 따라 움직인 뒤 슬라이드 전환한다.
- 복용 탭 선택 표시는 흰색 배경·검정 글자·옅은 그림자로 한다. 주간/월간 달력 모두 왼쪽 스와이프로 다음 달, 오른쪽으로 이전 달을 표시한다. 시간 설정 대신 편집/완료 버튼과 드래그 순서 변경 손잡이를 제공하며 저장 순서는 홈에도 반영한다. 일반 모드에서 항목을 왼쪽으로 밀면 삭제 버튼을 노출하고 확인 후 삭제한다. 알림에는 전체/건강/변화/알람 필터를 제공한다.
- 알림 벨은 독립 알림 화면으로 이동한다. 건강/변화/알람 분류 버튼과 아이콘·본문·날짜의 세로 목록을 제공하며 체험용임을 표시한다. 선택 분류를 다시 누르면 전체 목록으로 돌아온다. 건강 점수 카드 전체가 분석으로 이동하는 링크이며 링은 12시부터 반시계방향으로 72%까지 차오른다. 모션 감소 설정에서는 즉시 최종 상태를 표시한다.
- 2026-09-22 홈 피드백: 펼친 월간 달력은 왼쪽 스와이프 시 다음 달, 오른쪽은 이전 달로 이동한다. 건강 점수의 안내 아이콘은 건강 변화 추이(분석 상단)로 이동하는 화살표로 교체한다. 홈 복용 목록은 스크롤바 없이 손가락/마우스 드래그로 넘기는 카드 스와이프이며 모든 등록 항목을 표시한다. 스와이프 시 복용 체크가 오작동하지 않도록 한다.
- 인트로는 건너뛰기·다시보기 없이 자동 재생한다. 온보딩 로고는 제공된 로고 모션의 최종 정지 형태를 사용한다. 버튼은 무채색으로 통일하고 CTA 옆 장식용 이동 화살표는 제거한다. 뒤로가기·달력 조작 화살표와 영양 상태 색상은 유지한다.
- 홈 월간 달력의 우측 버튼은 '전체보기'이며 선택 날짜를 유지한 채 복용 탭으로 이동한다. 달력 펼치기/접기는 화살표만 표시하고 위쪽 막대는 제거한다.
- 기준 자료: 사용자가 제공한 `비타미.zip`의 15개 화면과 `vitame-intro.html`.
- 시작: 로고 모션 → 온보딩 3장 → 로그인 체험 → 영양제 등록 → 등록 정보 확인 → 홈. 새 방문마다 시작 흐름을 표시하며 기존 데이터는 유지한다.
- 온보딩은 스와이프·페이지 버튼·건너뛰기를 지원한다. 2·3장의 미확정 문구와 로그인은 체험용 콘텐츠로 보완한다.
- 홈: 날짜·스캔·알림, 펼치는 주간/월간 달력, 건강 점수, 부족 안내, 오늘의 복용 순서. 기존 인사·AI 배너·인사이트는 홈에서 제외한다.
- 복용: 매주/매월 전환. 매월 선택 시 7열 월간 달력이 본문 안에서 펼쳐져 전체 스크롤 길이가 늘어난다. 이전/다음 달과 날짜 선택을 지원하며 선택 날짜의 복용 기록을 표시한다. 관리 상세의 시간 수정·삭제를 유지한다.
- 일반 배경·글자·아이콘과 버튼은 무채색을 유지한다. 캐릭터는 아주 연한 민트, 카드 표면은 연한 민트·하늘·라벤더·크림으로 표현한다. 건강 점수와 영양 분석의 상태·차트 색상은 유지한다.
- 분석: 별도 요약/상세 단계 대신 `영양소 상태 / AI 가이드` 두 탭. 상태 탭은 변화 추이와 과다·부족·유지 목록, 가이드 탭은 건강 부위별 상태와 인사이트·생활 습관·맞춤 제품 추천 연결을 제공한다. 기존 nutrients 주소는 상태 탭으로 연결한다.
- 쇼핑: 추천 요약, 제품 TOP 3, 가로 슬라이드 배너. 주의 성분 제품은 비교용임을 명시한다. 제품 상세는 이미지·수량·장바구니·제품 정보/성분/리뷰 탭을 유지한다.
- MY: 프로필, 통계, 내 정보/관심 건강, 영양제 관리, 리포트, 찜, 안내 메뉴.
- 시안의 회색 문구 자리, Frame 텍스트, 휴대폰 상태 표시줄은 실제 UI에 넣지 않는다.
- 실제 인증·OCR·결제는 기존 MVP 제외 범위를 유지한다. 이번 작업은 로컬 수정이며 배포는 별도 요청에 따른다.

## 1. 프로젝트 개요

| 항목 | 내용 |
| --- | --- |
| 프로젝트명 | VITAME |
| 서비스 유형 | 개인 맞춤형 영양제 관리 모바일 웹 |
| 제작 버전 | 모바일 웹, 화면 너비 430px 이하 |
| 제작 언어 | HTML5, CSS3, Vanilla JavaScript |
| 핵심 경험 | Scan → Analyze → Guide → Manage |

VITAME는 사용자가 복용 중인 영양제를 간편히 등록하고, 섭취 데이터를 바탕으로 영양 상태를 이해하며, 필요한 관리 행동까지 이어갈 수 있도록 돕는 개인 맞춤형 영양 관리 서비스다. 사용자가 복잡한 성분표와 중복 섭취를 직접 계산하는 대신, 등록·분석·가이드·복용 관리를 하나의 흐름으로 제공한다.

> **필수 구현 규칙**
>
> 모든 구현 및 수정 세션은 코드 작성·수정 전에 반드시 이 `PROJECT_PRD.md` 전체를 읽고 숙지한다. 구현 순서는 `Read PRD → Understand Current Structure → Inspect Existing Code → Implement`를 따른다. 기존 코드와 PRD가 충돌하면 PRD 및 사용자가 가장 최근에 확정한 요구사항을 우선하며, 임의로 화면·기능·플로우를 추가하거나 바꾸지 않는다.

## 2. 문제 정의와 목표

여러 영양제를 함께 복용하는 사용자는 제품별 성분과 함량, 서로 다른 단위(mg·μg·IU), 중복 섭취 가능성을 직접 비교해야 한다. 건강 정보는 많지만 사용자는 자신에게 무엇이 부족하고 과한지, 어떻게 관리해야 하는지 판단하기 어렵다.

VITAME는 다음의 전환을 목표로 한다.

```text
복잡한 영양 정보 → 간편 등록 → 데이터 통합 → 상태 시각화 → 관리 행동
```

### 목표

- **Simple**: 복잡한 영양 성분 정보를 이해하기 쉬운 상태 정보로 제공한다.
- **Fast**: 스캔과 최소 입력으로 등록 및 확인 부담을 줄인다.
- **Safe**: 부족뿐 아니라 중복·과다 섭취 가능성도 함께 인지하게 한다.

사용자는 서비스 안에서 다음 세 질문에 답을 얻어야 한다.

1. 지금 내 상태는 어떤가?
2. 왜 이런 결과가 나왔나?
3. 그래서 어떻게 관리하면 되는가?

## 3. 핵심 사용자 가치

- 영양제 라벨 스캔으로 성분 입력 노동을 줄인다.
- 영양 점수와 상태 요약으로 현재 상태를 빠르게 파악한다.
- 부족·주의·적정 영양소를 분리해 복잡한 데이터를 해석한다.
- 분석 결과를 AI 최적화 가이드와 맞춤 제품 탐색으로 연결한다.
- 매일의 복용 여부를 간단히 기록해 지속적인 관리를 돕는다.

## 4. 대상 사용자

### 다수 영양제 복용자

여러 영양제를 복용하며 중복·과다 섭취를 걱정하는 사용자. 전문 지식보다 현재 복용 방식이 적절한지 빠르게 확인하는 경험을 원한다.

### 건강관리 입문자

건강 관심은 높지만 성분 비교와 직접 기록에 부담을 느끼는 사용자. 간편한 등록, 직관적인 상태 확인, 개인화된 관리 방향을 원한다.

## 5. 정보구조(IA)

```text
VITAME
├── 온보딩
├── 영양제 등록
│   ├── 카메라 스캔
│   ├── 이미지 선택
│   ├── 직접 입력
│   └── 스캔 결과 확인
├── 홈 대시보드
│   ├── 오늘의 건강 점수
│   ├── 주요 영양 상태
│   ├── 오늘의 복용
│   ├── AI 맞춤 가이드 진입
│   └── 건강 인사이트
├── 복용 관리
│   ├── 날짜 선택
│   ├── 복용 목록 및 예정 시간
│   └── 복용 완료 기록
├── 영양 분석
│   ├── 주요 영양소 상태
│   └── AI 최적화 가이드
├── 맞춤 제품 추천
│   └── 제품 상세
└── MY
    ├── 내 정보 관리
    ├── 관심 건강 관리(선택)
    ├── 등록 영양제 관리
    ├── 복용 이력 및 건강 리포트
    ├── 찜한 제품
    ├── 알림·계정 설정
    └── 고객센터
```

> `관심 건강 선택`은 초기 온보딩의 필수 단계가 아니다. 필요 시 사용자가 **MY → 내 정보 관리 → 관심 건강**에서 선택적으로 추가·수정한다. 초기 분석의 핵심 데이터는 등록된 영양제와 섭취 데이터다.

## 6. 사용자 플로우

### 재방문 시작 화면 (2026-09-18)

- 사이트를 새로 열거나 새로고침하면 기존 등록 여부와 관계없이 온보딩부터 표시한다. 저장된 홈 주소로 접속해도 동일하다.
- 온보딩의 시작 및 샘플 체험은 모두 영양제 스캔 화면으로 연결한다.
- 등록 영양제, 복용 기록, 관심 건강 등 기존 저장 데이터는 유지한다.
- 같은 방문 내에서 하단 메뉴를 이동할 때는 온보딩을 반복하지 않는다.

### 홈 상단 수정 확정 (2026-09-18)

- 홈 상단에는 선택 날짜와 영양제 스캔·영양 분석·알림 아이콘을 배치한다.
- 기본 상태는 주간 달력이다. 달력 영역을 아래로 당기거나 펼치기 버튼을 누르면 월간 달력이 열린다.
- 월간 달력은 이전/다음 달 이동, 오늘로 돌아오기, 날짜 선택, 접기를 지원한다.
- 선택 날짜의 복용 기록을 홈에 표시하고 해당 날짜에 복용 체크를 저장한다. 미래 날짜에는 복용을 기록하지 않는다.
- 본문 순서는 기존 시안의 영양 점수 → 부족 영양소 안내 → 복용 → AI 가이드 → 건강 인사이트를 유지한다.
- 하단 메뉴는 홈·복용·분석·쇼핑·MY를 유지한다. 이번 수정은 로컬 검토 후 별도로 배포한다.

```text
온보딩
  ↓
영양제 등록(스캔 / 이미지 선택 / 직접 입력)
  ↓
스캔 결과 확인 및 수정
  ↓
등록 완료
  ↓
홈 대시보드
  ├─ 복용 관리
  ├─ 영양 분석
  │    ↓ 주요 영양소 상태 클릭
  │  주요 영양소 상태
  │    ↓ AI 최적화 가이드 보기
  │  AI 최적화 가이드
  │    ↓ 맞춤 제품 추천 보기
  │  맞춤 제품 추천
  │    ↓ 제품 선택
  │  제품 상세
  ├─ 맞춤 제품 추천 → 제품 상세
  └─ MY
```

분석 파트는 화면 역할이 겹치지 않도록 다음의 깊이 구조를 유지한다.

| 단계 | 화면 | 사용자 질문 | 역할 |
| --- | --- | --- | --- |
| What | 영양 분석 | 현재 상태는 어떤가? | 점수·요약·추이를 제공 |
| Why | 주요 영양소 상태 | 왜 이런 상태인가? | 부족·주의·적정 영양소 상세 제공 |
| How | AI 최적화 가이드 | 어떻게 관리할까? | 주의·보완·유지 행동 제안 |
| Action | 맞춤 제품 추천 | 무엇을 선택할까? | 필요 영양소 기반 제품 탐색 |

## 7. 화면별 요구사항

| 화면 | 목적 | 필수 구성 및 동작 |
| --- | --- | --- |
| 온보딩 | 서비스 가치 전달 및 시작 | VITAME 소개, 시작 CTA. 관심 건강 선택을 강제하지 않는다. |
| 영양제 등록(스캔) | 입력 부담 최소화 | 카메라 스캔, 앨범 이미지 선택, 직접 입력 진입을 제공한다. |
| 스캔 결과 | OCR/입력 결과 검토 | 제품명, 주요 성분, 함량, 1일 섭취량, 섭취 주기·시간을 확인·수정하고 `이대로 등록하기`로 저장한다. |
| 홈 대시보드 | 서비스 허브 | 인사, 오늘의 건강/영양 점수, 부족·적정·주의 요약, 오늘의 복용, 인사이트, 상세 진입을 제공한다. 상세 데이터를 과밀하게 나열하지 않는다. |
| 복용 관리 | 일상 복용 기록 | 날짜, 완료 수/전체 수, 달성률, 영양제 목록, 예정 시간, 완료 토글을 제공한다. 최소 동작으로 기록 가능해야 한다. |
| 영양 분석 | 전체 상태 요약 | 영양 점수, 종합 상태, 부족·주의·적정 개수, 주요 영양소 상태, 복용 지속률, 변화 추이, 상세 진입을 제공한다. |
| 주요 영양소 상태 | 결과 원인 상세 | 부족·주의/과다·적정 영양소와 섭취 수준을 구분하고, 하단 `AI 최적화 가이드 보기` CTA를 제공한다. |
| AI 최적화 가이드 | 관리 행동 제안 | `주의`, `보완 필요`, `유지`로 행동을 구조화한다. 식단·운동·수면·수분 등 일반적 생활관리 콘텐츠를 보조로 제공할 수 있으며 `맞춤 제품 추천 보기`로 연결한다. |
| 맞춤 제품 추천 | 필요 기반 제품 탐색 | 부족 영양소, 등록 제품, 분석 결과, 선택 설정을 근거로 추천한다. 카드에 이미지, 제품명, 핵심 영양소, 특징, 가격, 상세 진입을 제공한다. |
| 제품 상세 | 제품 비교·확인 | 제품 이미지, 성분·함량, 특징, 가격, 찜·장바구니 등 목업 행동을 제공한다. |
| MY | 개인 데이터와 설정 관리 | 내 정보, 선택형 관심 건강, 등록 영양제, 복용 이력, 리포트, 찜, 알림, 고객센터, 계정 설정을 제공한다. |

## 8. 기능 요구사항

### 영양제 등록

- 스캔·이미지 선택·직접 입력의 세 경로를 제공한다.
- 스캔 후에는 자동 저장하지 않고 결과 검토·수정 단계를 거친다.
- 등록 완료 후 홈과 복용 관리, 분석 데이터에 반영한다.

### 복용 기록

- 영양제별 복용 완료 상태를 토글할 수 있다.
- 완료 수와 달성률은 즉시 갱신한다.
- 날짜 선택에 따라 해당 날짜의 기록을 표시한다.

### 영양 분석과 가이드

- 영양 상태는 `부족`, `주의/과다`, `적정`으로 명확히 구분한다.
- 영양 분석에서 주요 영양소 상태를 클릭해 상세 화면으로 이동한다.
- 상세 화면의 CTA는 AI 최적화 가이드로, 가이드 CTA는 맞춤 제품 추천으로 이어진다.
- 건강 정보는 참고용이며 질병 진단·치료처럼 표현하지 않는다.

### 탐색과 설정

- 쇼핑 영역과 AI 가이드 모두 맞춤 제품 추천에 진입할 수 있다.
- 제품 카드에서 제품 상세로 이동한다.
- 관심 건강은 MY에서 선택적으로 관리하며, 미선택 상태에서도 핵심 서비스 플로우가 정상 작동해야 한다.

## 9. UI/UX 원칙

- **Mobile first**: 최대 430px 폭을 기준으로 설계하며 작은 화면에서도 가로 스크롤 없이 동작한다.
- **요약에서 상세로**: 홈과 분석 메인은 핵심 상태를 요약하고, 상세 정보는 다음 화면에 위임한다.
- **입력 최소화**: 자동 인식 결과를 수정하는 방식으로 직접 입력을 줄인다.
- **명확한 상태 표현**: 색상과 텍스트/아이콘을 함께 사용한다. 색상만으로 상태를 전달하지 않는다.
- **일관된 액션**: 주요 CTA는 다음 단계가 명확한 동사형 문구를 사용한다.
- **신뢰 가능한 톤**: 의학적 확정·진단 표현을 피하고, 데이터 기반 참고·관리 안내로 표현한다.

### 비주얼 가이드

- White 기반, VITAME Green을 Primary Color로 사용한다.
- Light Green 배경과 라운드 카드 UI를 사용한다.
- 충분한 여백과 친근한 캐릭터/일러스트를 활용한다.
- Green은 적정/정상, Yellow·Orange는 주의, Red는 부족 또는 확인 필요 상태에 사용한다.
- 로그인 이후의 주요 화면에는 `홈 | 복용 | 분석 | 쇼핑 | MY` 하단 내비게이션을 제공하고 활성 탭을 명확히 표시한다. 온보딩·최초 등록 중에는 숨길 수 있다.

## 10. 기술 요구사항

### 기본 기술 스택

```text
HTML5
CSS3
Vanilla JavaScript
```

- 프레임워크는 MVP 기본 구현 범위에 포함하지 않는다.
- 공통 UI는 재사용 가능한 CSS 클래스와 JavaScript 함수로 관리한다.
- PC에서 열어도 콘텐츠 컨테이너는 최대 430px로 제한해 중앙에 배치할 수 있다.
- 화면별 콘텐츠 높이를 강제 통일하지 않으며, 긴 콘텐츠는 자연스러운 세로 스크롤을 사용한다.

```css
.app {
  width: 100%;
  max-width: 430px;
  min-height: 100dvh;
  margin: 0 auto;
}
```

### 권장 구조

```text
/
├── index.html
├── pages/
├── css/
│   ├── reset.css
│   ├── common.css
│   └── pages.css
├── js/
│   ├── app.js
│   ├── data.js
│   └── components.js
└── assets/
    ├── images/
    └── icons/
```

## 11. 데이터 및 상태 예시

MVP는 Mock Data와 Mock Interaction으로 구현할 수 있다. 실제 OCR, AI 분석, 의료 데이터, 결제 API가 없어도 화면과 플로우 검증이 가능해야 한다.

```javascript
const appState = {
  profile: {
    name: "김비타",
    healthInterests: [] // MY에서 선택적으로 관리
  },
  supplements: [
    {
      id: "supp-001",
      name: "비타민 D",
      ingredients: [{ name: "비타민 D", amount: 1000, unit: "IU" }],
      schedule: { frequency: "매일", time: "아침" },
      takenToday: false
    }
  ],
  nutrition: {
    score: 72,
    nutrients: [
      { name: "비타민 D", intakeRate: 43, status: "부족" },
      { name: "오메가-3", intakeRate: 112, status: "주의" },
      { name: "마그네슘", intakeRate: 56, status: "부족" },
      { name: "비타민 C", intakeRate: 88, status: "적정" }
    ]
  }
};
```

## 12. MVP 범위

### 반드시 구현

- 온보딩
- 영양제 등록 UI 및 스캔 UI
- 스캔 결과 확인·수정·등록
- 홈 대시보드
- 복용 상태 변경
- 영양 분석
- 주요 영양소 상태 상세
- AI 최적화 가이드
- 맞춤 제품 추천 및 제품 상세
- MY
- 화면 간 이동 및 하단 내비게이션
- 430px 이하 모바일 화면 대응

### MVP 제외 범위

- 실제 OCR 서버 연동
- 실제 AI 분석 API 및 의료 데이터 연동
- 회원 인증 서버
- 실제 결제·배송·쇼핑몰 백엔드
- 실제 푸시 알림
- 의료 진단, 처방, 치료 기능

제외 기능은 사용자 경험 검증을 위한 더미 데이터와 목업 상호작용으로 대체한다.

## 13. 구현 규칙

1. 구현 시작 전 `PROJECT_PRD.md` 전체를 읽는다.
2. 제작 기준은 **430px 이하 모바일 웹**이며, 구현 언어는 **HTML/CSS/JavaScript**다.
3. 관심 건강 선택을 초기 필수 플로우로 복구하지 않는다.
4. 분석 화면의 역할을 중복하지 않는다: 영양 분석(요약) → 주요 영양소 상태(상세) → AI 최적화 가이드(행동) → 맞춤 제품 추천(탐색).
5. 모든 정보를 한 화면에 과도하게 배치하지 않으며, 요약 → 상세 구조와 세로 스크롤을 사용한다.
6. PRD에 없는 대규모 기능을 임의로 추가하지 않는다.
7. 검증되지 않은 건강 정보를 확정적 진단·치료처럼 표현하지 않는다.

## 14. 완료 조건

- [ ] 온보딩부터 영양제 등록, 스캔 결과 확인, 홈 진입까지의 흐름이 작동한다.
- [ ] 홈에서 복용 관리, 영양 분석, 맞춤 제품 추천, MY로 이동할 수 있다.
- [ ] 영양 분석 → 주요 영양소 상태 → AI 최적화 가이드 → 맞춤 제품 추천 → 제품 상세 흐름이 끊기지 않는다.
- [ ] 복용 완료 상태와 달성률이 화면에서 갱신된다.
- [ ] 관심 건강을 초기 단계에서 요구하지 않으며, MY에서 선택적으로 관리할 수 있다.
- [ ] 모든 화면이 430px 이하에서 가로 넘침 없이 읽히고 조작 가능하다.
- [ ] 상태 정보가 색상 외 텍스트 또는 아이콘으로도 전달된다.
- [ ] 건강 관련 문구가 진단·치료를 약속하지 않고 참고용 관리 안내로 표현된다.

### 최신 상호작용 수정
- 홈 월간 달력의 전체보기 자리를 오늘 버튼으로 교체한다.
- 복용 오늘 버튼은 주간/월간 선택 오른쪽에 고정하고 달력 스와이프와 분리한다.
- 복용, 분석, 쇼핑, MY 제목은 화면 중앙에 정렬하고 상단 아이콘은 20px SVG로 통일한다.
- 복용 항목 오른쪽 스와이프 시 왼쪽 편집 버튼을 노출해 해당 영양제 상세설정으로 연결한다. 수정 시 기존 ID와 복용 기록은 유지한다.

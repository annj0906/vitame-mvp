# VITAME 구현 안내

## 2026-09-18 로컬 수정

재접속 및 새로고침 시 저장된 등록 여부나 URL 해시와 관계없이 온보딩으로 진입합니다. 시작 버튼과 샘플 체험 버튼은 모두 스캔 화면으로 연결합니다. 기존 저장 데이터는 유지됩니다. 로컬에서 재접속과 시작 → 스캔 이동을 확인했습니다.

2026-09-18 재배포 완료: 사용자가 기존 프로젝트 화면에 폴더를 드롭한 후, 5개 파일을 확인하고 Production으로 배포했습니다. 배포 ID: `13Hs8GYGoJxZQ8nEZhmQQr6Qh5Jg`. 기존 공개 주소 https://vitame-beryl.vercel.app 에서 재접속 → 온보딩 → 시작 → 스캔 동작을 확인했으며 브라우저 오류가 없었습니다.

홈 상단 달력은 `public/vitame/home-calendar.js`와 `home-calendar.css`에 구현했습니다. index.html에서 함께 불러옵니다. 다음 배포에는 이 두 파일도 포함해야 합니다.

주간/월간 전환, 아래로 드래그해 펼치기, 날짜별 복용 표시, 월 이동, 오늘 복귀, 스캔·분석·알림 바로가기를 브라우저에서 확인했습니다. 320px 가로 넘침 없음, JavaScript 문법 검사 통과. 2026-09-18 Vercel Production에 반영했습니다.

## Vercel 배포

- 공개 주소: https://vitame-beryl.vercel.app
- 관리 페이지: https://vercel.com/annj0906-1891/vitame
- 배포 방식: Vercel Drop to Deploy로 `public/vitame`의 HTML/CSS/JavaScript 5개 파일 직접 배포.
- Git 자동 배포는 연결하지 않았습니다. 파일 수정 후에는 다시 배포해야 공개 사이트에 반영됩니다.
- 공개 주소의 온보딩 표시 및 등록 화면 이동 확인 완료.

실제 앱은 `public/vitame/index.html`, `style.css`, `app.js`로 구성된 순수 HTML/CSS/JavaScript 모바일 웹입니다. `index.html`을 브라우저로 직접 열어 실행할 수 있습니다. 기존 Next.js 실행 환경은 이 정적 앱을 표시하는 진입점으로만 사용합니다.

- 최대 콘텐츠 폭: 430px. 스마트폰 프레임과 운영체제 상태 표시줄은 제외했습니다.
- 온보딩 → 영양제 등록 → 결과 수정 → 홈 → 복용 / 분석 / 쇼핑 / MY가 연결됩니다.
- 분석 → 주요 영양소 상태 → AI 최적화 가이드 → 맞춤 제품 추천 → 상세가 연결됩니다.
- 등록 제품, 날짜별 복용 상태, 이름, 선택 관심 건강, 찜, 장바구니는 브라우저 localStorage에 저장됩니다.
- 관심 건강은 MY의 선택 설정입니다.
- 분석 72점 및 상태(부족 4 / 주의 2 / 적정 4)는 일관된 예시 데이터입니다. 사용자 제품 변경에 따른 실제 영양 계산은 하지 않습니다. 등록 제품은 홈·복용·MY에 반영됩니다.
- 사진 촬영/선택은 브라우저 파일 입력을 사용합니다. 실제 OCR 대신 직접 입력하거나 샘플 라벨 결과를 체험할 수 있습니다.
- 샘플 체험 버튼으로 네 가지 영양제와 오늘의 3/4 복용 완료 상태를 불러올 수 있습니다.
- 실제 계정 인증, 의료 분석, 알림 발송, 결제, 배송은 제공하지 않습니다.

모든 구현 세션은 먼저 PROJECT_PRD.md 전체를 읽어야 합니다.

## 검증 결과

Vercel에서는 루트의 `vercel.json` 설정으로 `public/vitame`을 정적 사이트로 배포합니다. 설치와 프레임워크 빌드는 생략하며, 공개 경로 `/`에서 모바일 웹이 시작됩니다. 설정 근거: https://vercel.com/docs/project-configuration/vercel-json

- 브라우저: 샘플 라벨 → 결과 편집 → 등록 → 홈 반영, 복용 체크 → 완료 수 갱신, 전날의 별도 기록 확인.
- 분석 → 영양소 상세 → AI 가이드 → 추천 → 제품 상세 이동 확인.
- 찜, 수량 2개 선택, 장바구니 합계 44,000원 확인.
- MY의 선택 관심 건강 저장 및 새로고침 후 유지, 복용 기록 유지 확인.
- 320px 화면의 홈·추천·영양소 상세 및 430px 추천 화면에서 가로 넘침 없음.
- JavaScript 문법 검사 및 TypeScript 검사 통과. 검사 중 브라우저 오류 로그 없음.
- Next.js production build: 코드 컴파일 성공. 이후 TypeScript 작업 프로세스 생성에서 환경 권한 오류 `spawn EPERM`으로 중단. 별도로 실행한 TypeScript 검사는 성공.
- 실물 카메라 촬영과 실제 외부 API 연동은 검증 범위에서 제외.
